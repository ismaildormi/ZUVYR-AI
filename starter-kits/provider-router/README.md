# Provider Router Starter

Run:

```bash
npm start
```

This kit uses local mock providers only. It demonstrates capability filtering, health gates, cost/latency ranking and ordered fallback without requiring a paid API.

Replace the mock adapters with your own providers and keep all credentials server-side.
