# Permissioned Tool Starter

Run:

```bash
npm start
```

The example demonstrates a grant that is separate from ownership. Try changing the action, expiry, resource ID or `revokedAt` value to see explicit denial reasons.

For production, persist grants and audit events server-side and re-check permission immediately before execution.
